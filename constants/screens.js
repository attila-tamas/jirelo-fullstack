export const screens = {
  xs: 0,
  sm: 360,
  md: 540,
  lg: 800,
  xl: 1280,
  xxl: 1536,
};

const UNIT = "px";

export const screens_px = {
  xs: screens.xs + UNIT,
  sm: screens.sm + UNIT,
  md: screens.md + UNIT,
  lg: screens.lg + UNIT,
  xl: screens.xl + UNIT,
  xxl: screens.xxl + UNIT,
};
